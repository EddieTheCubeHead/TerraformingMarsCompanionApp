package com.example.terraformingmarscompanionapp;

import com.example.terraformingmarscompanionapp.game.events.GameEvent;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

/**
 * A unit test for the basegame configuration. Has examples of a few edge-cases like playing a card
 * that both triggers arctic algae and removes plants added by arctic algae and raising oxygen to
 * raise temperature to place an ocean.
 */
public class BasegameUnitTest {

    // Create a fake game and players for the game

    // All moves of the game
    private ArrayList<GameEvent> play_order = new ArrayList<>(Arrays.asList(

    ));
}
